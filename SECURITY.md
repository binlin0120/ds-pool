# 安全说明

## 本项目不含凭据

- 部署所需的密钥与账号写在服务器上的 `/opt/ds-pool/pool.env`（权限 600），
  仓库中只有占位符样例 `pool/pool.env.example`。
- `_creds/`、`*.env`、`server_ssh.env` 等敏感文件均在 `.gitignore` 中，不会被提交。

## 上报漏洞

如果发现可能泄露敏感信息或可被利用的安全问题：

- **不要**在公开 Issue 中张贴日志、地址、账号等原始信息；
- 推荐使用 GitHub 的私有漏洞上报功能提交；
- 若只能在公开 Issue 中描述，请先脱敏，并注明「涉及安全隐患」。

## 使用提醒

本项目面向自用与学习，请遵守 DeepSeek 及你所使用服务的使用条款；请勿将号池能力用于
绕过平台限制、批量牟利或任何违法违规用途。
